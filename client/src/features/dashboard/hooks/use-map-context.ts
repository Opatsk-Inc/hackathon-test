/**
 * useMap hook extracted to separate module for Fast Refresh compatibility.
 * Re-exports the hook from map.tsx to avoid React Fast Refresh warnings.
 */

export { useMap } from "@/components/ui/map"
